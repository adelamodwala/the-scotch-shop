platform: linux
image_resource:
  type: docker-image
  source: {repository: ymedlop/npm-cache-resource, tag: latest}

inputs:
  - name: source-code
    path: .

run:
  path: concourse/tasks/install.sh
